<h1>Artist Form</h1>

<%= form_for @artist do |f| %>

<%= f.label :name, "Artist Name" %>
<%= f.text_field :name%>

<%= f.label :bio, "Artist Bio" %>
<%= f.text_area :bio %>

<%= f.submit "Create Artist" %>

<% end %>
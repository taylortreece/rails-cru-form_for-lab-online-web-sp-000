<h1>Edit Artist</h1>

<%= form_for @artist do |f| %>

<%= f.label :name, "Artist Name" %>
<%= f.text_field :name%>

<%= f.label :bio, "Artist Bio" %>
<%= f.text_area :bio %>

<%= f.submit "Update Artist" %>

<% end %>